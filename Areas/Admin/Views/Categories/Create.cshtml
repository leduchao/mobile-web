@model MobileWeb.Models.DTOs.CategoryDTO

@{
	ViewData["Title"] = "Tạo mới danh mục";
}

<h1 class="text-dark">Tạo danh mục mới</h1>
<hr />
<form asp-action="Create">
	<div asp-validation-summary="ModelOnly" class="text-danger"></div>

	<div class="form-group w-25">
		<label asp-for="Name" class="control-label text-dark"></label>
		<input asp-for="Name" class="form-control" />
		<span asp-validation-for="Name" class="text-danger"></span>
	</div>

	<button type="submit" class="btn btn-outline-primary text-uppercase d-inline">Tạo</button>
	<span class="m-2">|</span>
	<a asp-action="Index" class="d-inline">Back to List</a>
</form>

@section Scripts {
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}
	}
